<div class="section">
  <div class="container">
    <div class="{{ class }}">
      {% for item in items %}
        <div class="{{ item.class }}">
          {{ item.content }}
        </div>
      {% endfor %}
    </div>
  </div>
</div>
